# Supabase(Phase 2 /trades)。兩個都是【公開值】,可安全放進前端 bundle。
# 🔴 service_role key 絕對不放這裡、不放 Vercel、不進 repo —— 它會繞過 RLS。
PUBLIC_SUPABASE_URL=https://<your-project>.supabase.co
PUBLIC_SUPABASE_ANON_KEY=<anon key>
