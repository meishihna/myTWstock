# Phase 2 — 延後驗證清單

**這份文件存在的理由**:某一輪裡「記為未驗證」的項目,如果只寫在當時的回報裡,
會在輪次交替時蒸發 —— 下一輪沒有人記得它還欠著,而它看起來像已經做完了。
**所有「本輪無法驗證」的項目一律登記在此,直到它被驗證或被明文放棄。**

規則:
1. 每一項都要有**為什麼這一輪驗不了**、**要誰做什麼才驗得了**、**排在哪一輪**
2. 驗證完成後**不刪除**,改標 ✅ 並附驗證日期與證據 —— 刪掉就失去「曾經欠過」的紀錄
3. 「已驗證」不可只憑「應該沒問題」;要有實跑結果

---

## 待驗

### ⏳ Google OAuth 端到端(排:**選做輪**,不擋上線)

**2026-08-14 調整**:由輪 4 移出,改為之後的獨立小輪。
理由:magic link 已端到端驗過,跨帳號隔離驗證用兩個 email 即可完成 ——
Google 是**方便性功能,不是上線條件**,而它需要 user 先在 Google Cloud 建立憑證。
**不讓方便性擋住上線。**

⚠️ **移出輪次不等於消失。** 這一項仍然未驗證,仍在本清單上。

**輪 1 驗到的**:點擊按鈕會呼叫 `signInWithOAuth({ provider: "google" })`,
且回傳 error 時畫面會顯示訊息。

**輪 1 沒驗到的**:**沒有真的走完一次 Google 登入。**
本機 Supabase 堆疊未啟用 Google provider,而啟用需要:

| 誰 | 做什麼 |
|---|---|
| user | 在 Google Cloud 建立 OAuth 2.0 用戶端(取得 client id / secret) |
| user | 在 Supabase 主控台 → Authentication → Providers 啟用 Google 並填入 |
| user | 在主控台的 Redirect URLs 加入 `https://my-twstock.vercel.app/auth/callback` |
| 我 | 線上實跑一次:點 Google → 完成授權 → 回跳 → `/trades` 顯示帳號 |

⚠️ 我**不能代為建立帳號或輸入憑證**,這幾步必須由 user 執行。

### ⏳ 正式站 redirect 允許清單(排:輪 4)

本機的允許清單寫在 `supabase/config.toml`(已加 `localhost:4330/auth/callback`),
**正式站的清單在 Supabase 主控台**,不在這個 repo 裡 —— 兩者不會自動同步。
未加入時的症狀:magic link 的 `redirect_to` 會被退回 `site_url`,
使用者點了信卻回到錯的頁面。**輪 1 就是這樣才發現的。**

### ⏳ Vercel 環境變數(排:輪 3 上線前)

`PUBLIC_SUPABASE_URL`、`PUBLIC_SUPABASE_ANON_KEY`(兩者皆為公開值)。
未設定時 `/trades` 會顯示「登入尚未開通」並維持唯讀預覽(fail closed,輪 1 已實測)。
🔴 `service_role` key **不放這裡、不放 repo、不放任何伺服器路由**。

### 🔴 執行順序:先跑隔離自測,再輸入真實持股

`/trades/rls-selftest` 的 15 條探針裡有兩條是**寫入型**的:
新增一筆 `user_id = peer` 的交易、刪除 peer 的交易。

| 隔離狀態 | 結果 |
|---|---|
| 正常 | 兩者都被資料庫擋下,什麼也不會發生 ✅ |
| **有洞** | **peer 的資料被真的改動或刪除,不可復原** |

也就是說,**這個探針在真的找到問題的那一刻,就同時造成了損害** ——
而這一頁存在的理由正是「萬一那些測試是錯的」,它必須假設自己可能找到東西。
**一個找到問題時會毀掉問題現場的探針,是設計上的缺陷。**

目前的處置(未重做探針,成本考量):
1. 頁面上有明顯警語,說明會送出真實的寫入與刪除嘗試
2. **文件與操作說明一律寫死順序:先跑隔離自測 → 確認通過 → 再輸入真實持股**

> **重訪條件:若日後要在有真實資料的帳號上例行重跑此頁。**
> 屆時應把寫入型探針改成不會造成損害的形式
> (例如寫入一筆可辨識的哨兵列再自行清除、或改用 `count` 而非 `delete … returning`)。

### ✅ 線上跨帳號隔離複驗 —— 已完成(2026-08-14)

本機 42 項已全過,但**測試環境成功推論不到正式環境**(pg 版本、既有 schema、角色權限都可能不同)。
線上版需要兩個真實帳號,**由 user 執行**;我提供獨立自測頁(需明確參數才啟動、
只吃一個已知的 `user_id`、無連結入口)。

**結果見下方「已驗證」節:11/15(= 目前上限)、0 項失敗。**

---

## 部分驗證 / 刻意不重驗

### fail closed(缺環境變數時的行為):**本機已驗,線上未再驗**

本機實測:移除 `PUBLIC_SUPABASE_URL` / `PUBLIC_SUPABASE_ANON_KEY` →
`/trades` 顯示「尚未設定 Supabase 連線(缺 …)」,**不退化成「看起來像未登入」**。

**線上刻意不重驗。** 那段程式碼路徑在 production 沒有任何本機沒有的差異,
重驗的資訊量趨近於零;代價卻是動到對外可見的線上站,復原還要再跑一次部署。
**用可能弄壞線上站的方式,去重驗一個已經驗過的字串,不划算。**

> **重訪條件:若日後改動 `supabaseClient.ts` 的環境變數讀取邏輯,需重驗。**

### ⏳ `PUBLIC_SUPABASE_URL` 的**值**無人驗過

監督者核對過 Vercel 上的**變數名稱**與程式碼一致,但該變數被標為 Sensitive、讀不回值。
若 project ref 有誤,症狀是登入時出現連線錯誤 —— **看得見、不靜默**,所以不擋上線。
**但這一格確實無人驗過,記在此處。** 首次線上登入成功即等同驗證。

---

## 🔴 近失事件:自測頁在對照組 2/5 的情況下印出 ✅(2026-08-14)

**為什麼要寫下來**:線上複跑永遠不會再產生這次假通過 —— 我們現在知道要先補資料,
複跑必然是誠實結果。「對照組 2/5 卻印 ✅」只發生過一次,不寫下來就永久消失,
還會被複跑的漂亮結果吸收成「一路都好」。
**近失事件比成功記錄更難保存,因為它沒有造成後果。**

### 首跑的完整輸出形狀

15 條攻擊查詢全部 0 列,對照組:

| 對照項目 | 實際 | 當時判定 |
|---|---|---|
| `trades` 讀自己 | 3 列 | 通過 |
| `cash_flows` 讀自己 | **0 列** | 無法判定 |
| `watchlist` 讀自己 | **0 列** | 無法判定 |
| `preferences` 讀自己 | **0 列** | 無法判定 |
| `profiles` 讀自己 | 1 列 | 通過 |

**頁面仍印出「✅ 隔離成立 … 對照組有 2/5 項讀得到自己的資料」。**
而且當時 **7 個檢視連對照組都沒有** —— 它們的 0 列一點證據力也沒有。

### 根因:一個「在正確與錯誤假設下都會通過」的檢查

`cash_flows` 攻擊 0 列、對照 0 列 —— **「RLS 有效」與「RLS 完全沒開」的輸出一模一樣。**
把「對照組存在」當成「對照組有效」,是兩件不同的事。

### 兩個錯,分開記

**① 轉述比程式碼嚴格(較嚴重的那個)**
我在給監督者的操作說明裡寫「對照組若不是 5/5,我不會宣告通過」;
輪 4 報告裡寫的是「若對照組一項都沒讀到」;**程式碼實作的是後者**(`ctrlOk > 0`)。
監督者是照前者批准的 —— 那個保證從來不存在。

> **守則:回報守衛條件時,轉述必須逐字對應程式碼,不得轉述「我打算做到」的版本。**
> 一個沒實作的保證比沒有保證更糟:它會讓審核者停止追問。

**② 判準用總數而非逐項** —— 已改為逐項配對。

### 修正

| | |
|---|---|
| 判準 | 攻擊 0 + 對照 ≥1 → 通過;**攻擊 0 + 對照 0 → 無法判定**;攻擊 >0 → 失敗 |
| 檢視 | 7 個全部補上對照 |
| 判準位置 | 抽成純函式 `src/lib/selftestVerdict.ts`,頁面 import 同一支 —— 否則測到的判準不是頁面用的 |
| 注入測試 | `tests/selftest-verdict.mjs` 13 項,含**首跑那個形狀的迴歸案例**(2 通過 + 3 對照 0 → partial 而非 pass) |

### 哨兵對照:這次唯一一個「主動推廣」的修正

原本「寫入 peer 被拒 → 通過」有同一個病:**被拒可能只是因為寫入根本不通**。
所以補上哨兵對照 —— 在自己帳號寫入一筆可辨識的列(`0000` / `1990-01-01`),
證明寫入路徑本身可行,再自行刪除;刪除探針同理(對照 = 刪掉自己的哨兵列)。

**推廣的邏輯**:凡是「期望失敗」的檢查,都要另外證明「它在期望成功時會成功」。
否則「失敗了」與「根本沒跑起來」無法區分 —— 這與 4b 對照組是同一條原則,
只是套用在寫入方向而不是讀取方向。

---

## 🔴 線上可達到的鑑別力上限:11/15(不是 15/15)

自測頁的 15 項裡,有 4 項**目前的介面根本無法補上資料**:

| 項目 | 為什麼補不了 |
|---|---|
| 表 `cash_flows` | **沒有任何 UI 會寫入這張表**(現金流輸入尚未實作) |
| 表 `watchlist` | `/watchlist` 用的是 **localStorage**,不寫 DB 的 watchlist 表 |
| 表 `preferences` | 沒有偏好設定 UI |
| 檢視 `v_cash_balance` | 由 `cash_flows` 推導,同上 |

另有 1 項需要**刻意製造壞資料**才能取得鑑別力:
`v_position_anomalies` —— 正常帳號不該有賣超;要驗它必須故意賣超過買入量(UI 做得到)。

> **所以:⚠️「部分成立 11/15」就是目前的滿分,不是失敗。**
> 看到黃色警告不代表隔離有問題 —— 要看的是**有沒有任何一項是「失敗」**。
> 只要 0 失敗,且無法判定的項目都在上表之內,結果就是好的。

> **重訪條件:現金流 / 觀察清單 / 偏好設定的 UI 上線時,上限自動提高到 15/15,屆時重跑。**

---


## 已驗證

### ✅ 線上跨帳號隔離複驗(2026-08-14,production,兩個真實帳號)

`https://my-twstock.vercel.app/trades/rls-selftest?peer=<B>`,由 user 執行。

```
表 trades                 攻擊 0 / 對照 5    通過
表 cash_flows             攻擊 0 / 對照 0    無法判定
表 watchlist              攻擊 0 / 對照 0    無法判定
表 preferences            攻擊 0 / 對照 0    無法判定
表 profiles               攻擊 0 / 對照 1    通過
檢視 v_trade_lots         攻擊 0 / 對照 5    通過
檢視 v_ticker_sold        攻擊 0 / 對照 2    通過
檢視 v_open_lots          攻擊 0 / 對照 3    通過
檢視 v_holdings           攻擊 0 / 對照 1    通過
檢視 v_position_anomalies 攻擊 0 / 對照 1    通過   ← 刻意製造賣超後升級
檢視 v_realized_lots      攻擊 0 / 對照 2    通過
檢視 v_cash_balance       攻擊 0 / 對照 0    無法判定
無條件 select trades      他人 0 / 自己 5    通過
寫入 peer 的列            已拒(42501)/ 哨兵寫入成功   通過
刪除 peer 的列            影響 0 列 / 哨兵刪除 1 列    通過

⚠️ 部分成立 11/15 —— 這就是目前的上限,0 項失敗。
```

**11/15 是滿分**,不是缺陷。4 項無法判定的**結構性原因**(不是資料沒填):

| 項目 | 為什麼補不了 |
|---|---|
| `cash_flows` | 目前**沒有任何介面會寫入這張表**(現金流輸入尚未實作) |
| `watchlist` | `/watchlist` 的自選股存在 **localStorage**,不寫 DB 的 watchlist 表 |
| `preferences` | 沒有偏好設定介面 |
| `v_cash_balance` | 由 `cash_flows` 推導,同上 |

> **重訪條件:上述任一張表出現輸入介面後,補跑這一頁。**
> 三張表都有介面時,上限即為 15/15。

### ✅ 事前預測 15/15 命中 —— 記下做法本身,不只記結果

在 user 執行**之前**,逐項預測了 15 格各會是「通過」還是「無法判定」,
以及整體會是 ⚠️ 部分成立;並明寫兩格不確定(`v_holdings`、`v_realized_lots`)
與它們各自的分支條件。實跑結果**逐項相同**。

**為什麼要先預測**:預測對了,那一跑的證據力遠大於「跑完再解釋」——
因為「事後說得通」對任何結果都做得到,「事前說得準」不行。
預測錯了也有價值:那代表對判準或資料形狀的理解有缺口,而那個缺口會被指出來。

實際發生過一次小的預測偏差,而且**歸因正確**:首次 user 加的是買進而非賣出,
於是 `v_ticker_sold` 未升級 —— 那是資料形狀與假設不符,不是隔離缺陷。

### ✅ 監督者獨立補上的兩條佐證(比自測頁本身更強)

**① `trades.id` 序號跳號 → 證明「被拒」發生在資料庫,不是瀏覽器端假造**
Postgres 的序號不隨交易回滾歸還,**被拒的 insert 一樣吃掉一個號**。
實測 `trades.id` 缺 5 —— 那個號正是被 RLS 退回的那筆寫入用掉的。
這排除了「頁面在前端自己印『已拒絕』而根本沒送出請求」的可能。
**自測頁說自己被拒,和資料庫證明它被拒,是兩回事。**

**② peer 的列逐列完好,不只筆數**
刪除探針跑完後確認 peer 的資料仍是 `id 4 | 2317 | buy | 500` ——
比對的是**內容**而不是**筆數**(筆數相同但內容被改動,只數筆數看不出來)。
先前擔心的「刪除探針會造成損害」,實測結果是**零損害**。

---

## 🔁 移交下一輪(本輪不處理)

記在此處只為**不讓它在輪次交替時蒸發**,本輪不動。

### `cash_flows` / `preferences` 沒有輸入介面 —— 同一個缺口,三個症狀

`/trades` 上的「合計損益 —」、「現金水位 —」、「資產配置空白」是**同一個原因**:
沒有現金流資料,就算不出總權益,也就沒有配置分母。
三個症狀分開修會做三次白工;要修的是那一個缺口。

### 同頁兩區塊對同一事實不一致 —— 未解

「個股走勢與買賣點」左側顯示「尚無持股」,但**同一頁的現有持倉有 2330**。
同一份事實在同一頁上有兩種說法。**尚未查明原因,不可當成渲染時序打發掉** ——
在查清楚之前,任何解釋都只是猜測。


## 已知限制(Phase 2 /trades)

### 🔴 取現價時,持股代號會離開瀏覽器

**架構承諾**是「client 直連 Supabase,伺服器從不看到使用者資料」。
但取得現價必須呼叫本站的 `/api/quote-batch`,而**持股代號清單在網址的 query string 裡**
→ 會進入 Vercel 的存取記錄。累積起來就是綁在 IP 上的持股輪廓。

**這不是缺陷**(要即時報價就避不開),**但不可以是隱形的**。
`/trades` 的隱私聲明已明文寫出:「取得現價時,你持有的股票代號會傳送到本站的報價代理」。

**架構承諾與實際資料流不一致時,要改的是承諾的措辭,不是把差異藏起來。**

已查證的邊界(2026-08-14,讀 `src/pages/api/quote-batch.ts`):
| 項目 | 事實 |
|---|---|
| 記錄 | 該路由**沒有任何 `console.*` 或寫檔** |
| 持久儲存 | 無。只有單筆、60 秒 TTL 的**行程內記憶體**快取(鍵 = 排序後的代號字串) |
| 離開瀏覽器的內容 | **只有代號**。股數、成本、交易明細、損益都不會送出 |
| 回應標頭 | `Cache-Control: public, max-age=60` —— 內容以網址為鍵,不會跨使用者混淆 |

> **重訪條件:僅在改成「瀏覽器直抓報價」或「改用 POST 讓代號不進網址」時。**
> 後者成本低(存取記錄只留網址、不留 body),但 `/api/quote-batch` 是多頁共用的既有端點,
> 改動要另開一輪評估影響面 —— **不在 Phase 2 UI 主線範圍內**。

### 現價取不到時的呈現:留白,不以 0 代替

Yahoo 失敗、代號下市、報價延遲都會讓現價缺失。那時未實現損益是**未知**,不是 **0**。
- 該列的現價欄顯示「現價未取得」,市值/未實現/報酬率一律留白
- **缺價個股不併入總市值的分母**,頁尾標明「不含 N 檔未取價個股」

這是資料層那條 `null` ≠ `0` 在呈現層的延伸:**0 是資料,未知是不存在。**
實測(2026-08-14):代號 `9999` 格式合法但無報價 → 三欄留白、頁尾出現「不含 1 檔未取價個股」。

> **重訪條件:不重訪 —— 這是原則,不是待修項。**
