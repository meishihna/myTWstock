# 財務資料 — 已知限制(結案清單)

**判定日期:2026-08-11** · 量測基準:`web/public/data/financials/` 1,975 檔(115 ingest 後,commit `69966993f`)

---

## 判定原則

七天的財務線打磨在此收束。分級規則:

| 分級 | 處置 |
|---|---|
| **顯示錯誤數字給使用者** | 開輪修(例:季度費用堆疊圖的負段被靜默丟棄、總高被高估 → 已修,`f9d858cd5`) |
| **資料誠實但不完整** | 記入本文件,不修 |
| **缺少解釋性標示** | 記入本文件,不修 |

「不完整」與「錯誤」的分界:**空白是誠實的,錯誤的數字不是。**
本文件列出的每一項,呈現出來的數字都是對的;缺的是完整度或解釋。

⚠️ **不在本文件範圍**:21 格「解析缺口」(官方頁面有值、交付檔為 null)**正在由引擎側修 parser**,
不是已知限制。逐格證據見 `web/tests/fixtures/lost-cells-classified-20260811.json`(`verdict: "解析缺口"`)。

---

## 一、資料層面

### 1.1 去累計傳染:14 格 / 12 檔

**機制**(以 3147 民國109年第2季研發費用為例,官方頁面逐格核對):

```
官方 ajax_t164sb04 給的是【累計】,單季由相減得出:
    Q2單季 = 累計(Q2) − 累計(Q1)

3147 的 109Q2 頁面:研究發展費用 = 5,887 仟元(累計)          ← 有
3147 的 109Q1 頁面:頁面存在,但【沒有研究發展費用那一列】   ← 累計(Q1) 未知
  → 相減算不出來 → 該季交付 null
```

14 格全部是這個形狀:**自己那期的頁面有累計值,前一期的頁面存在但缺該科目列。**

**為什麼不補救**:唯一的補法是把「前一期沒有那一列」當成 `0`,然後 `Q2單季 = 累計(Q2) − 0`。
那違反本專案一路守住的 **`null` ≠ `0`**(見 `financials-contract.md`「🔴 `null` ≠ `0`」)。
「該公司當期未揭露此科目」與「該科目金額為零」是兩件事,把前者寫成 0 會製造一個
看起來正常、實際上是我們發明的數字。**寧可留白。**

若日後要處理,是獨立議題(需要先決定「未揭露」能否在特定條件下視為 0),不屬財務線範圍。

### 1.2 語義 null:21 格 / 8 檔

官方該期的損益表**根本沒有那一列**(例:1604 的管理費用在 2018–2019 各期皆無此列)。
`null` 是正確呈現,不是缺漏。

### 1.3 🔴 同一家公司會跨年度改變科目揭露方式

**1612 華新的實例**:`管理費用` 在民國 113 年度頁面有列(13,434 仟元),
114 年度頁面**沒有那一列**。同一檔、同一科目,前者是解析缺口(已交引擎修)、後者是語義 null。

**推論(對日後任何 parser 或稽核都成立)**:
不能以「公司」為單位判定「這家公司有沒有這個科目」,必須**逐期別**判定。
以公司為單位的白名單/黑名單,會在改制那一年整批判錯。

### 1.4 金融業 CAPEX 全空:39 檔 / 1,521 個期別格

`industryType` 為 `fh` / `bank` / `ins` / `sec` 的個股中,39 檔的 `capex` 在**所有期別**皆為 null。
官方個別公司現金流量表未提供對應科目 → 留白正確。

> 引擎側回報的數字為「41 檔 / 656 格 / 2.3%」,與本文件的 39 檔 / 1,521 格差異來自口徑
> (本文件計「該檔所有期別列數」,引擎側計的是另一種分母)。兩者都指同一件事,不影響結論。

### 1.5 保險業 IFRS 17:6 檔

`industryType === "ins"` 共 6 檔。IFRS 17 之後保險業損益表結構與其他業別不同,
本站的欄位映射不涵蓋其特有科目 → 部分欄位留白。

### 1.6 現金流量級異常大:1,067 格 / 97 檔

`|ocf| / |icf| / |fcf|` 超過該期營收 50 倍者。多為投資公司、控股公司、營收極小但現金部位大的個股 ——
**數字本身沒有錯**,只是「相對營收」這個比較基準對它們沒有意義。
已量測、寫入本文件,**不追**。

---

## 二、呈現層面(數字都是對的,缺的是解釋性標示)

### 2.1 三種「不可比」缺少標示

| 情形 | 規模 | 現況呈現 |
|---|---|---|
| **季度費用出現負值**(官方追溯重編/重分類) | **122 檔 / 196 格**(報告頁範圍) | 負段畫在 0% 基線下方,總高 == 三段真實和(已修正,`f9d858cd5`);**未標示**這是重分類所致 |
| **營收 ≤ 0 時比率不適用** | 負營收 **65 格 / 32 檔**、零營收 **66 格 / 26 檔**(交付檔全範圍;有值營收共 80,914 格) | 比率欄**留白**(`ratioSeries` 對分母 ≤ 0 回 null);**未標示**「營收為負,比率不適用」 |
| **量級 > 10× 不可比** | 22 格(引擎側量測,本站**未複驗**) | 照實顯示;**未標示** |

三者的共同點:**呈現的內容是誠實的**(留白或如實畫出),缺的只是一句解釋。
使用者若不知道背景,可能會困惑,但不會被誤導成錯誤的數字。

### 2.2 費用堆疊圖:最外層髮絲段看不見

堆疊的**最外層**段使用精確高度、不套 1px 可見性下限 —— 這是為了讓
「畫出來的總高 == 三段真實和」精確成立(下限套在最外層會把端點推出去,實測誤差達 0.26pp)。

代價:最外層若是髮絲段(< 1px,約 < 0.3% 營收)會小到看不見。
**實例**:1465 的 2020Q4 管理費用率 −0.0642%,畫成 0.198px。
數值仍完整呈現在 tooltip;總高精確。影響範圍精確算出為 **9 檔**
(1325 / 1438 / 1618 / 1742 / 3056 / 3115 / 3521 / 3530 / 3661)。

內層段仍套 1px 下限(內層溢出不足 1px 只與相鄰段重疊,不影響端點)。

---

## 三、工程層面

### 3.1 `.astro` frontmatter 沒有型別閘

`astro check` 需要把 `@astrojs/check` 加進 devDependencies,而本專案**刻意零 devDependencies**
(工具一律 `npx --yes`)。因此 `.astro` 檔的 frontmatter 目前不在 `tsc --noEmit` 的涵蓋範圍。

替代證據(以 `FinancialDashboard.astro` 的堆疊圖幾何為例):
`.ts` 模組的 `tsc --noEmit`、production build、dev server 與**建置產物**的逐格量測(誤差 0)。

低優先待辦:把 `stackedExpensePctSvg` 抽到 `src/lib/` 成為 tsc 可檢的模組。
**等它下次要改時一起做** —— 這段幾何邏輯目前已由多層實測背書,型別閘的邊際價值不高。

---

## 附:量測方式

本文件的數字除註明「引擎側量測」者外,皆由本站在 2026-08-11 自行清點,定義如下:

- **負費用格**:adapter 輸出的季度區塊中,`rev > 0` 且三費任一 `< 0` 的格(報告頁 1,737 檔範圍)
- **負/零營收格**:交付檔 `annual` + `quarters` 兩區塊中 `rev` 為有限值且 `< 0` / `== 0` 的格(1,975 檔全範圍)
- **金融業 CAPEX 全空**:`industryType ∈ {fh, bank, ins, sec}` 且 `capex` 在所有期別皆非有限值
- **現金流量級**:`rev > 0` 且 `|ocf|` 或 `|icf|` 或 `|fcf|` `> 50 × rev`

⚠️ 掃描範圍會改變數字:1,737 檔(有報告頁)與 1,975 檔(交付檔)相差約 15%。
引用本文件的數字時請一併引用範圍。
