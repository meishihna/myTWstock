__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.DS_Store
*.tmp
*.log
_temp_fix_*.py
enrichment*.json
.claude/settings.local.json

# Node modules（根目錄 & web 子目錄）
node_modules/
web/node_modules/
web/dist/
web/.astro/
.env
.env.*
!.env.example
# FinMind token（本機操作用，絕不上傳）
.finmind_token

# MOPS 全市場季報快取（可很大）
data/mops_cache/*.json

# 編輯器／工具暫存雜物
*.Rhistory
.claude/scheduled_tasks.lock

# --- Phase 2: 停止追蹤可重生／冗餘產物（歷史仍保留；縮小 .git 需另做 history rewrite）---
# 一次性財務備份
data/financials_store_backup_*/
# predev/prebuild 會重生的索引（避免每次 commit 的 diff 噪音）
web/public/data/reports-index.json
web/public/data/wikilink-hub-top500.json
web/public/data/wikilink-stubs.json
web/public/data/themes-index.json
web/public/data/sector-stats.json
web/public/data/screener-index.json
web/public/data/map-index.json
web/public/data/momentum.json
# /news-digest 技能的暫存輸入(每次重生;news-digest.json 本身為內容,需追蹤)
web/news-snapshot.json
# 註:web/public/data/financials/ 曾是「舊財務鏡像」(可由 financials_store 再生)而不追蹤。
#     現在同一路徑改放【官方 MOPS 季報/年報/資產負債表】(1,974 檔),
#     那是【無法由本 repo 再生】的交付物,且 build-screener-index 依賴它 —— 必須追蹤,
#     否則線上 build 會靜默退回 yfinance,改動變成無效。故移除該忽略規則。
web/public/data/industries-index.json
web/public/data/search-index.json

# ── 回測引擎防線(本 repo 為【公開】repo)────────────────────────
# 引擎程式碼一律不得進入此 repo;/signals 只讀「算好的資料 JSON」。
# 若下列路徑出現,幾乎肯定是誤複製,直接忽略以免外流策略邏輯。
engine/
signals_engine/
backtest_core.py
backtest*.py
run_combo*.py
twse_chips.py
*.ipynb
tw-stock-public/
# 引擎原始輸出(過大/非本站使用者需要):只保留 web/public/data/signals/ 下的成品
data/signals_raw/
